# OWASP ZAP 予備設定ファイル (TOWMEI ERP用)
# スキャンの重要度や無視するルールをここで定義できます。

# 10020: X-Frame-Options Header Not Set (低)
# 10021: X-Content-Type-Options Header Not Set (低)
# 10054: Cookie Without SameSite Attribute (低)

# [形式]: <RuleID> <Action> <Notes>
# Action: IGNORE (無視), INFO (情報のみ), LOW (低), MEDIUM (中), HIGH (高)

10020	IGNORE	(Next.js がデフォルトで処理することが多いため)
10021	IGNORE	(Next.js がデフォルトで処理することが多いため)
10054	IGNORE	(開発環境では SameSite が設定されないことが多いため)
